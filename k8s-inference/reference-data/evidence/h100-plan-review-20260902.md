# H100 reference-data root review — 2026-09-02

This receipt records the read-only root review of task commit
`5ab8bd7e54181da8616102012a6baa694f244da4` against the authoritative H100
deployment. The plans were generated from the isolated task worktree and
protected run root. No Terraform plan was applied, no Kubernetes object was
changed, no dataset was downloaded, and the out-of-scope accelerator target
was not queried or mutated.

## Current live foundation

The already approved task-owned infrastructure remains:

- one 2 TiB shared filesystem;
- one private, versioned 2 TiB bucket;
- one dedicated regular CPU node group;
- one `cpu-d3` / `8vcpu-32gb` worker with a 160 GiB network boot disk,
  conservative schedulable capacity of 7,000 millicores, 28,672 MiB memory
  and 114,688 MiB ephemeral storage, and the exclusive
  `workload.fs2.nebius/reference-data=true:NoSchedule` taint.

The 2 TiB reference filesystem/object-store sizing covers the approximately
630 GB expanded official AlphaFold3 bundle with more than 1 TiB of headroom.
The staging Job's 6 CPU / 24 GiB request fits the dedicated worker contract;
it is not eligible for the shared system node. The manager independently
confirmed that the superseded orphan node group is `NotFound`, with no
remaining live residue. Exact cloud identities remain only in private Task
Deck evidence.

## Source and contract gates

- Portable root, direct-stage and example defaults are `disposable` with
  `forbid_deletion=false`, selecting complete reverse-order teardown when the
  versioned bucket is empty.
- The authoritative live configuration explicitly opts into `retain` with
  `forbid_deletion=true`, matching the already-created storage. Its contract
  reports `full-stack-destroy-incomplete-infrastructure-retained` and exports
  the IDs required for deliberate adoption; it does not promise full destroy.
- Reference workloads are fixed to the dedicated `fs2-reference-data`
  namespace. Foundation continues to own `fs2-data`, and no reference
  NetworkPolicy can select the live database Pods.
- Reference filesystem attachment is a per-accelerator-pool feature. The two
  active H100 pools explicitly opt in; a future heterogeneous pool defaults
  to no attachment.
- The status Deployment becomes service-ready through `/healthz`; dataset
  readiness remains separate on `/readyz`, `/v1/status`, and Prometheus.
- Private MSA remains the default. Runtime images are resolved through the
  configured regional registry delivery contract, and immutable GPU inputs
  contain only validated content-addressed references.

## Offline verification

All tests ran from the isolated task worktree at the reviewed commit:

| Gate | Result |
|---|---|
| Root Python contracts | 140 passed |
| Infrastructure Python contracts | 82 passed |
| Reference-data Python contracts | 15 passed |
| Infrastructure mocked-provider fixtures | 5 passed |
| Reference-module mocked-provider fixtures | 3 passed |
| `terraform fmt -check -recursive` | passed |
| Terraform validation | root, infrastructure, foundation, workloads and reference-data roots passed |

The provider fixtures enforce the exact optional retained/disposable
reference-data address, type and count contracts. They include fresh
empty-volume apply/teardown, enabled-reference plan/state, namespace/network
policy, health/readiness separation, pipeline identity and the corrected
23-GPU/seven-single-GPU-node full-catalog expectations.

## Fresh read-only plans

The wrapper generated these mode-0600 artifacts at 2026-09-02 23:57 UTC:

| Plan | SHA-256 | Exact disposition |
|---|---|---|
| `configuration.tfplan` | `07ec263338eccbbc96eedae48fc191f96ee0f5ec24fce6782f43bd84b464a976` | No managed-resource action; output-only reference lifecycle additions |
| `configuration.plan.json` | `6510afc9f65ae557652869364d3f7da70e36c2ed013836defa7d392ac9fa5120` | Machine-readable root review |
| `infrastructure-plan.tfplan` | `25412de2ac20c9c00b9aa82b6ea333cf5d24818607c67dfa9c3e077a7d3d52cf` | 0 add, 1 in-place update, 0 destroy, 0 replacement |
| `infrastructure-plan.plan.json` | `87edc5b4cd617f58bed986aea2448aa834a5aeaaa660eb547d2c164752335844` | Machine-readable infrastructure review |

The sole infrastructure resource action is an in-place update to
`nebius_storage_v1_bucket.reference_data[0]` that removes one legacy
unsupported bucket setting if applied. The bucket identity, 2 TiB limit and
enabled versioning remain unchanged. Terraform reports no replacement path.
Every other managed resource is `no-op`, including the reference filesystem,
dedicated CPU node group, system node group, both H100 node groups, cluster,
registry and IAM resources.

The infrastructure output changes update the source commit and add the
truthful retained destroy-completion fields. The root output change adds the
same lifecycle fields to the normal effective-configuration surface. These
are state-output changes, not live service mutations.

The wrapper correctly stopped at the infrastructure convergence gate. It did
not generate new foundation or workloads plans, so the old downstream plans
remain rejected and must not be applied. In particular, no
`fs2-reference-data` namespace, Kueue object, status service, pipeline Job,
MSA workload or AlphaFold3 staging workload has been deployed.

## Root decision required

This successor is ready for root review of the single in-place bucket update.
Do not apply this plan, reuse an older downstream plan, or start bulk staging.
After an explicit approval and convergence of this infrastructure update,
regenerate foundation/workloads from the new state and independently reject
any unrelated shared-service action before considering another apply.
