# H100 reference-data plan review — 2026-09-02

This receipt records the manager-gated plan-only pass from task commit
`e543fdff4272953ee7c60b80fd07b78e96298e6c`. The target was
`project-e00rene`, region `eu-north1`, cluster
`mk8scluster-e00j5z9te7x5dd9g6a` (`k8s-inference-h100`). No B300 target was
queried or mutated.

## Gate and disposition

- The earlier infrastructure apply is the only approved live mutation in this
  pass. It created the dedicated 2 TiB filesystem, private versioned 2 TiB
  bucket and scoped access identity, then attached the filesystem to the
  existing system and H100 node templates.
- No root configuration, new infrastructure, foundation or workload plan was
  applied during this review pass.
- The new reference-data namespace and workload resources remain absent.
- A dedicated reference-data CPU pool is now mandatory in the root contract.
  Downstream inputs cannot become valid until that pool has been independently
  reviewed, applied and observed in infrastructure state.

## Fresh root and infrastructure plans

The wrapper was run from the isolated task worktree with the live mode-0600
tfvars and run root. It stopped after finding the expected infrastructure
change.

| Plan | SHA-256 | Review |
|---|---|---|
| `configuration.tfplan` | `b3a6532ea18de36d1cc6cbf3d70edbc2b0141dc2d99ed75fb9f54fc99fcaac42` | One state-only contract update; not applied |
| `configuration.plan.json` | `72f37afab9fe6f8d234f818e57f8ff4bfd925bb4e9794943cc26eb9703fba678` | Machine-readable root review |
| `infrastructure-plan.tfplan` | `6b36c4129627ccb50d4eb0040851b795e5d80c6ba166af1215cd2c43ada166aa` | Exactly 1 create, 0 update, 0 delete; not applied |
| `infrastructure-plan.plan.json` | `0e61e48e55ef0330af35c742ceeabbad42d6a80554a5a6fcb1ca2f70636d38ef` | Machine-readable infrastructure review |

The sole infrastructure action is
`create nebius_mk8s_v1_node_group.reference_data[0]`:

- name `k8s-inference-h100-reference-data-cpu`;
- one regular `cpu-d3` / `8vcpu-32gb` node with a 160 GiB network boot disk;
- existing filesystem `computefilesystem-e00smrbs2e9zanhv56` attached as
  `fs2reference`;
- stable labels `workload.fs2.nebius/reference-data=true`,
  `capacity.fs2.nebius/pool=reference-data`, and
  `storage.fs2.nebius/reference-data=true`;
- `workload.fs2.nebius/reference-data=true:NoSchedule` taint;
- no GPU resource, local NVMe assumption, replacement or deletion.

## Rejected saved downstream plans

These plans predate the dedicated CPU-pool state and must not be applied:

| Saved plan | Timestamp (UTC) | SHA-256 | Reason rejected |
|---|---|---|---|
| `foundation-plan.tfplan` | `2026-09-02 21:57:24` | `18f1fbdc4d81d204d497ba6c9191378ef49be13ed4facc808f8a630f35772441` | Nine unrelated Kueue/OTel/Tempo/config-contract actions |
| `foundation-plan.plan.json` | `2026-09-02 21:57:24` | `6d5684b543db93685e7d2b21203cc3398b44383a1a2a873aafeedc89890f9ed3` | Machine-readable rejected plan |
| `workloads-plan.tfplan` | `2026-09-02 14:24:06` | `ceecb6e1b09d171c560cecad20a6e5154565d8df486a4662cad82b37b79c4bd6` | Stale zero-change plan, generated before this reference-data contract |
| `workloads-plan.plan.json` | `2026-09-02 14:24:08` | `3c4f89649bc7493eba715f28221f34c5512a3ce2306adbbb5d4ce357e2d74e67` | Machine-readable stale plan |

The rejected foundation plan contains updates to Kueue and OTel releases,
creates OTel cluster and Tempo releases plus a Grafana datasource, and replaces
shared cluster-contract readiness resources. Those actions belong to sibling
workstreams and are not authorized by this task.

## Live non-mutation checks

- The live node groups remain only `k8s-inference-h100-system`,
  `k8s-inference-h100-h100-1x`, and
  `k8s-inference-h100-h100-reserved-8x`; all report `RUNNING`.
- Namespace `fs2-reference-data` does not exist yet.
- The three current nodes report `Ready`; all carry the reference-data
  filesystem label from the approved attachment rollout.
- Control plane and admin Deployments are 2/2, PostgreSQL is 3/3, the hot Qwen
  Deployment is 1/1, and public `/readyz` plus `/admin/` return HTTP 200 with
  normal TLS verification.

## Required next review sequence

1. Review and explicitly approve a newly generated dedicated CPU-pool plan.
2. Apply only that bounded infrastructure plan and verify node labels, taint,
   filesystem mount and allocatable CPU/memory.
3. Regenerate, never reuse, foundation and workload plans from the updated
   infrastructure state.
4. Reject any new foundation/workload plan with an unrelated change; only then
   approve creation of the isolated namespace, private Kueue lane, status
   service and pinned resumable AlphaFold3 staging Job.
