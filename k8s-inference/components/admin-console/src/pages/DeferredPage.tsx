export function DeferredPage({ title, description }: { title: string; description: string }) {
  return <section className="state-panel state-panel--planned"><span className="eyebrow">Next vertical slice</span><h2>{title}</h2><p>{description}</p><p>No browser credential or placeholder number is exposed while its typed BFF capability is unavailable.</p></section>;
}
