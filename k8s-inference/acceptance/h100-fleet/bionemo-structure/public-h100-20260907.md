# H100 Bio/structure public acceptance — 2026-09-07

The seven-model cohort passed its public HTTPS and MCP acceptance. Each surface
ran two semantic requests, waited for the durable operation result, validated
the native response, and bound the operation to the intended live runtime.
The machine-readable result is `public-h100-20260907.json`.

| Model | HTTP | MCP | Final receipt |
| --- | --- | --- | --- |
| MolMIM | 2/2 PASS | 2/2 PASS | Bio r07 |
| GenMol | 2/2 PASS | 2/2 PASS | Bio r07 |
| ProteinMPNN | 2/2 PASS | 2/2 PASS | Bio r09 |
| DiffDock | 2/2 PASS | 2/2 PASS | Bio r09 |
| Boltz2 | 2/2 PASS | 2/2 PASS | Bio r07 |
| MSA Search PDB70 | 2/2 PASS | 2/2 PASS | Bio r08 |
| OpenFold2 | 2/2 PASS | 2/2 PASS | OpenFold2 r01 |

The response validators are byte-pinned in the runtime catalog. They check
model-specific meaning, not only JSON shape: RDKit-derived molecule scores,
amino-acid and probability invariants, finite/non-degenerate coordinates,
full ligand/protein identity, MSA query/hit identity, and finite protein
structures. Focused negative tests show the DiffDock native validator rejects
both a changed ligand and degenerate coordinates.

The failed denominators remain explicit. Bio r06 exposed a test-side mismatch
between source revision and the controller's dynamic route revision. Bio r07
then proved MolMIM, GenMol and Boltz2, while exposing a CPU-route helper error
for PDB70 and a direct-envelope/native-response mismatch for ProteinMPNN and
DiffDock. PDB70 passed in r08. The first envelope-only correction for
ProteinMPNN and DiffDock remained invalid in r08; r09 binds and executes their
pinned native validators and passes both transports. None of these corrections
changed a model image, weights, request resource limit, or semantic success
gate.

Full response bodies, operation objects and credentials remain only in the
mode-private receipt roots listed by the JSON report. This checked-in report
contains no credential or request/response body.
