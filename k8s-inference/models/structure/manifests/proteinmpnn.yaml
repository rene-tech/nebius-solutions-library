apiVersion: v1
kind: Service
metadata:
  name: proteinmpnn-b300
  namespace: fs2-models
  labels:
    app.kubernetes.io/name: proteinmpnn
    app.kubernetes.io/instance: proteinmpnn-b300
    app.kubernetes.io/part-of: fs2-serve
    fs2.nebius.ai/model-id: proteinmpnn
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: proteinmpnn-b300
  ports:
    - name: http
      port: 8000
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: proteinmpnn-b300
  namespace: fs2-models
  labels:
    app.kubernetes.io/name: proteinmpnn
    app.kubernetes.io/instance: proteinmpnn-b300
    app.kubernetes.io/part-of: fs2-serve
    fs2.nebius.ai/model-id: proteinmpnn
  annotations:
    fs2.nebius/compile-cache-abi: deployment-profile-abi-v1
    fs2.nebius/model-content-digest: sha256:c9cb4a671d79604111231f8dbfc7c590e06f1197453b7a6854ac6661a642f5bd
    fs2.nebius/runtime-image-digest: sha256:f27dda10178fb799dc8f75c2b7ced00643a6281fd3e37e95cd353700e6d62e38
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/instance: proteinmpnn-b300
  template:
    metadata:
      labels:
        app.kubernetes.io/name: proteinmpnn
        app.kubernetes.io/instance: proteinmpnn-b300
        app.kubernetes.io/component: model-runtime
        app.kubernetes.io/part-of: fs2-serve
        fs2.nebius.ai/model-id: proteinmpnn
      annotations:
        fs2.nebius/compile-cache-abi: deployment-profile-abi-v1
        fs2.nebius/model-content-digest: sha256:c9cb4a671d79604111231f8dbfc7c590e06f1197453b7a6854ac6661a642f5bd
        fs2.nebius/runtime-image-digest: sha256:f27dda10178fb799dc8f75c2b7ced00643a6281fd3e37e95cd353700e6d62e38
        fs2.nebius.ai/source-revision: 8907e6671bfbfc92303b5f79c4b5e6ce47cdef57
        fs2.nebius.ai/checkpoint-sha256: c9cb4a671d79604111231f8dbfc7c590e06f1197453b7a6854ac6661a642f5bd
    spec:
      automountServiceAccountToken: false
      enableServiceLinks: false
      tolerations:
        - key: dedicated
          operator: Equal
          value: fs2-inference
          effect: NoSchedule
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: runtime
          image: registry.example.invalid/k8s-inference/models/proteinmpnn-portable@sha256:f27dda10178fb799dc8f75c2b7ced00643a6281fd3e37e95cd353700e6d62e38
          imagePullPolicy: IfNotPresent
          env:
            - name: FS2_PORT
              value: "8000"
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          resources:
            requests:
              cpu: "4"
              memory: 16Gi
              nvidia.com/gpu: "1"
            limits:
              cpu: "16"
              memory: 128Gi
              nvidia.com/gpu: "1"
          startupProbe:
            httpGet:
              path: /readyz
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 360
          readinessProbe:
            # Startup already proves model readiness. Afterwards, keep the
            # endpoint routable while the single-flight GPU worker is busy.
            # Even TCP connects can exhaust the listen backlog during a long
            # inference, so probe the already-started PID without using its
            # request socket.
            exec:
              command: ["/bin/sh", "-c", "kill -0 1"]
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            # The exact adapter serializes inference on its HTTP thread; an
            # HTTP/TCP liveness probe would kill healthy long-running requests
            # when the listen backlog is full. Startup proves the model once;
            # after that, liveness only asserts that the runtime PID exists.
            exec:
              command: ["/bin/sh", "-c", "kill -0 1"]
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 4
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 8Gi
