apiVersion: v1
kind: Pod
metadata:
  name: fs2-namd-r20260923-l40s-worker
  namespace: fs2-models
  labels:
    app.kubernetes.io/name: namd-qualification
    scientific-ai.nebius.com/task: namd-r20260923
spec:
  restartPolicy: Never
  activeDeadlineSeconds: 14400
  automountServiceAccountToken: false
  nodeSelector:
    accelerator.fs2.nebius/pool-id: l40s-1x
    kubernetes.io/hostname: computeinstance-e00krzha55t0sg3t56
  tolerations:
    - key: dedicated
      operator: Equal
      value: fs2-inference
      effect: NoSchedule
  securityContext:
    runAsUser: 10001
    runAsGroup: 10001
    fsGroup: 10001
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault
  containers:
    - name: runtime
      image: cr.eu-north1.nebius.cloud/e00akg9ndpx77eaexh/fs2-platform/namd-worker@sha256:30df40215f7df047463cf8e37f1fb80457b73fbb7ae8f681b9cb40d980e5f14e
      command: [bash, -c]
      args:
        - |
          nvidia-smi --query-gpu=name,uuid,driver_version,memory.total,compute_cap --format=csv
          sleep 14000
      securityContext:
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: true
        capabilities:
          drop: [ALL]
      env:
        - name: CUDA_CACHE_PATH
          value: /work/cuda-cache
      resources:
        requests:
          cpu: "8"
          memory: 32Gi
          nvidia.com/gpu: "1"
        limits:
          cpu: "8"
          memory: 32Gi
          nvidia.com/gpu: "1"
      volumeMounts:
        - name: work
          mountPath: /work
        - name: tmp
          mountPath: /tmp
  volumes:
    - name: work
      emptyDir:
        sizeLimit: 64Gi
    - name: tmp
      emptyDir:
        sizeLimit: 1Gi
