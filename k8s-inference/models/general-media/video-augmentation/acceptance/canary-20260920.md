# Isolated video canary: not ready

The requested upload → cloudy preview → approve → bucket batch workflow is **not yet qualified**. The isolated workbench and real upload work; the Cosmos3 transfer prerequisites did not produce an acceptable cloudy clip. The quality gates stayed unchanged, no result was approved, and no batch was submitted.

## Deployed scope and immutable identity

User authorization: isolated Stockholm Scientific AI sandbox canary, dedicated user/key/bucket, provider configuration and paid test usage. Existing customer deployments were not replaced.

- Backend source integrated through `78f40fe81`; worker build source `f9812bcfea8b97c02fea5886431cd58eea78cde6`.
- Workbench source integrated through `1250a00`; runtime build source `94fed505a83de28609b4797bf050ceb305d1a16f`. Subsequent deploy-helper commit `8ca7b38` makes unrelated Tavily credentials optional; it does not change runtime bytes.
- Worker linux/amd64 manifest: `sha256:3500d15d8a04d74966377350e8a9cdf9546b3280dc63c1474b1b64251b8b488c`.
- Workbench linux/amd64 manifest: `sha256:3e5df6c2ec9318128b7f9cfde1b236cee8b0273befe84198860fb34b5de50f35`.
- Registry: `cr.eu-north1.nebius.cloud/e00akg9ndpx77eaexh`; worker repository `fs2-platform/fs2-video-augmentation`, workbench repository `lc`. Both published OCI indexes retain SLSA provenance and SPDX SBOM statements bound to their runtime manifests.
- The workbench uses short tag `lc:vc0920a`, verified against the above manifest, to satisfy the Serverless Compute image-label length constraint. This is a requested/registry image identity; it is not a claim of provider-signed runtime attestation.
- Endpoint `aiendpoint-e00m58m8ym56ky5f4m`, project `project-e00rene`, CPU-only 4-vCPU/16-GiB private-IP workbench. Dedicated tenant/principal `video-canary-20260920`, single user and separate bucket.
- PAIDF `bc5719362492a1e3b40bd7d33b43c46dd89efad5`; hosted Cosmos image `sha256:5e2680aa1f8332413638ec1bc962c3796a79a314c1c84f5456d32aa916839e32`; Cosmos model revision `7a312c868bcce8e40b3eb40861300a9d0ba3fde1`; vLLM-Omni `eb11446b7f2e30ca582f8aff3afe12e9a2e66f6c`.

The exact worker ran locally as UID10001. A loopback-only test translator used the ordinary canary key against the public native Cosmos API. It did **not** call actual private endpoints or mint a workload capability. This establishes worker/native-GPU prerequisite evidence only; hosted parent delegation, MCP/chat generation and batch remain untested live.

## Actual browser and bucket test

A real headless browser authenticated to the dedicated workbench, opened the Physical AI panel, selected an MP4 and uploaded it through the UI. The resulting S3 object was read back and matched all 2,183,869 source bytes, SHA256 `25da0db5381cccbc4f50c2ab25bf10b73cd4d411d8edecbde22fb660e3a24863`. An unauthenticated video API request returned401. The original upload remains intact.

The cloudy request was composed but deliberately **not sent** while App activation was blocked. There is no claim of successful agent tool selection, inline modified-video playback or human approval. An early deployment preflight failed before cloud creation because local AWS configuration lacked a `[default]` section; after an isolated config fix, the one endpoint was created successfully. The deployed helper retained an earlier warmup probe error; subsequent authenticated browser upload and final `/login` HTTP200 are the direct live checks.

## Real GPU results

Source is NVIDIA PAIDF's public `data/sample_input.mp4`. All preprocessing below was an explicit test-fixture preparation, not hidden product behavior.

| Case | Explicit input/settings | Outcome |
| --- | --- | --- |
| r1 | 1280×720, all153 frames,30FPS | Local test translator rejected an upload query. No native generation admitted. Harness fixed; failure retained. |
| r2 | Same720p clip, guidance7,35 steps | No output after more than10 minutes of first-attempt execution. Native platform reached attempt2; public cancellation confirmed. Root cause unresolved. |
| r3 | Center crop1440×1080, resize640×480,16FPS, first32 frames; guidance7, seeds42/43 | Two native outputs succeeded structurally, but motion scores0.4953 and0.5376 failed unchanged threshold0.682. Weather was not evaluated after motion rejection. |
| r4 | Same short fixture, guidance3, no PAIDF retry | HTTP402 budget guard before native generation. Caption/prompt calls had already run. |
| r5 | Same short fixture, guidance3, seed42, no PAIDF retry | Native output succeeded; all32 frames retained at640×480/16FPS. Motion0.869792 passed. Weather verifier answered clear, not requested overcast. Rejected. |

Native operation IDs and final public runtime attribution are retained in [the sanitized receipt](canary-20260920.json). All four admitted native operations are terminal: one cancelled and three succeeded at transport level. A native `succeeded` status is **not** a semantic pass.

The short source SHA256 is `748b75259042a1169be0721eb5e90a74cb163cb11b9be618b16c5951865ce748`. Final worker output hashes are `0395234f04d7e13c7fed9bf78f24828fb0c4c926b66c76a83da6c410fdacdcea` (r3 retry) and `1f25c38bfcd70c8bbad2d2b3e149365578d884e66dade8da80b0ac311e2103d1` (r5). Worker remuxed artifacts and native MP4s have separate byte identities.

Same-index source/output frame16 was visually inspected for r3/r5: vehicles, road details and foliage show substantial stylization/distortion. r5's higher motion score does not establish appearance preservation. Weather checking is a five-frame VLM heuristic, not ground truth; here it did not verify the requested change. No complete perceptual/physical-fidelity qualification is claimed.

The guidance3 comparison was motivated by [NVIDIA's pinned transfer example](https://github.com/NVIDIA/cosmos/blob/b0e54e88c322695dab188e6ed160c4d6d071c39d/cookbooks/cosmos3/generator/transfer/README.md). The [deployed vLLM-Omni recipe](https://github.com/vllm-project/vllm-omni/blob/eb11446b7f2e30ca582f8aff3afe12e9a2e66f6c/recipes/cosmos3/Cosmos3-Nano.md) also explains that a sole positive control weight is normalized to1; changing that weight would not change absolute control strength. Captions/prompts were regenerated and differed between r3/r5, so this was **not** a controlled one-variable causal experiment. Neither defaults nor thresholds were changed based on it.

During the 720p stall, the observed Pod was ready with no restarts, GPU utilization samples were 0%, memory was 37,521 MiB, and no clear error was logged. Cancellation was limited to this canary operation; shared Pod/snapshot configuration was not changed. Its public final runtime attribution is null; do not invent a joined GPU identity from the nearby Pod observations.

## Tests and final isolation audit

- Control-plane integration/regression suite:175 passed, including20 real local PostgreSQL tests. An initial stale-port connection failure is not counted as a pass; the corrected-port run passed.
- Exact-upstream fixture runtime suite:13 passed (fake generation/providers, separately labelled).
- Workbench video/workflow-draft Python tests:43 passed; video Node bridge tests:3 passed; deployment/network/helper tests:30 passed; production client build passed.
- Additive activation tests: 15 passed, including a regression proving the retained failed GPU receipt cannot activate the App. They cover existing rows, snapshot bundles, historical qualification identities, scheduler bytes, exact-worker evidence and real catalog/renderer compatibility.
- OCI manifest/statement subjects and metadata blob hashes verified for both images. The CPU control-plane helper's2-MiB metadata cap was insufficient for the worker's8-MiB SBOM; a canary-only64-MiB bounded reader verified it without changing production build policy.
- Final audit at2026-09-20T01:41:37Z: Helm release190 and Cosmos generation24; Helm values and Cosmos spec exactly unchanged. No new backend App/provider Secret/grant or CP rollout. No active canary GPU operation or held GPU reservation.
- Canary key has only `cosmos3-nano`, concurrency1, requests8/8, and expires2026-09-22T01:13:04Z. The self-imposed GPU admission cap was increased once from21,600 to25,200 for r5's10,800-unit reservation;18,000 conservative units were consumed and zero remain reserved. These are **not measured occupancy or a bill**. Request exhaustion prevents further generation; Token Factory charges are additional and not included in that ledger.

No resources or original media were deleted. The CPU workbench remains running, with its dedicated bucket and protected credentials. Raw evidence, auth state, logs and videos are retained under `/home/tux/secure-handoff/video-canary-20260920`; never commit credentials or signed URLs. `capture_canary.py` exports only allowlisted facts/hashes.

## Remaining decision and gates

Do not enable batch approval or publish this as a working weather editor. `activation/prepare.py` fails closed on the current receipt and preserves the live execution map when a future valid receipt exists.

The next work is backend/recipe quality qualification: either a separately scoped evaluation of the blueprint's Cosmos Transfer 2.5 backend, or a frozen, budgeted Cosmos3 transfer experiment with explicit success criteria and held-out clips. [NVIDIA's pinned PAIDF README](https://github.com/NVIDIA/paidf-augmentation/blob/bc5719362492a1e3b40bd7d33b43c46dd89efad5/README.md) documents Transfer 2.5 as its first video-transfer walkthrough. Neither option is proven to fix this failure. A new GPU backend/runtime is a material expansion and should be agreed before deployment.

After semantic success: recheck the then-current shared release, publish an integrated CP image, add the scoped provider Secret/App grants, exercise real hosted parent delegation, complete the actual chat preview and human-approved two-video bucket batch, then two unchanged clean customer cohorts. Do not reuse this failed canary as that proof.
