apiVersion: v1
kind: ServiceAccount
metadata:
  name: cosmos3-nano
  namespace: fs2-models
  labels:
    app.kubernetes.io/name: cosmos3-nano
    app.kubernetes.io/part-of: fs2-serve
automountServiceAccountToken: false
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cosmos3-nano-cache
  namespace: fs2-models
  labels:
    app.kubernetes.io/name: cosmos3-nano
    app.kubernetes.io/part-of: fs2-serve
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 64Gi
  storageClassName: compute-csi-default-sc
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cosmos3-nano-adapter
  namespace: fs2-models
  labels:
    app.kubernetes.io/name: cosmos3-nano
    app.kubernetes.io/part-of: fs2-serve
  annotations:
    fs2.nebius/output-contract: bounded-inline-json
    fs2.nebius/output-follow-up: object-backed-async-for-large-media
data:
  localize.py: |
    from __future__ import annotations

    import errno
    import fcntl
    import hashlib
    import json
    import os
    import shutil
    import stat
    import sys
    import time
    import uuid
    from concurrent.futures import ThreadPoolExecutor
    from pathlib import Path, PurePosixPath

    from huggingface_hub import snapshot_download

    RECEIPT_SCHEMA = "fs2-serve.nebius.ai/shared-cache-localization-receipt/v2"
    LOCK_PATH = Path(os.getenv("FS2_MODEL_LOCK_PATH", "/contract/model.lock.json"))
    CACHE_ROOT = Path(os.getenv("FS2_CACHE_ROOT", "/models"))
    HF_CACHE_ROOT = Path(
        os.getenv(
            "FS2_HF_CACHE_ROOT",
            str(Path(os.getenv("HF_HOME", str(CACHE_ROOT / ".cache" / "huggingface"))) / "hub"),
        )
    )


    def canonical_bytes(value: object) -> bytes:
        return (
            json.dumps(
                value,
                sort_keys=True,
                separators=(",", ":"),
                ensure_ascii=True,
                allow_nan=False,
            ).encode()
            + b"\n"
        )


    def load_model() -> dict[str, object]:
        lock = json.loads(LOCK_PATH.read_text(encoding="utf-8"))
        if lock.get("schema_version") != 2 or set(lock) != {"schema_version", "model"}:
            raise RuntimeError("unsupported model lock schema")
        model = lock["model"]
        required = {
            "id",
            "repo_id",
            "revision",
            "artifact_manifest_digest",
            "content_digest",
            "total_size_bytes",
            "files",
        }
        if not isinstance(model, dict) or set(model) != required:
            raise RuntimeError("model lock fields differ from the closed contract")
        if not all(
            isinstance(model[key], str) and model[key]
            for key in ("id", "repo_id", "revision")
        ):
            raise RuntimeError("model identity is invalid")
        for key in ("artifact_manifest_digest", "content_digest"):
            value = model[key]
            if (
                not isinstance(value, str)
                or len(value) != 64
                or any(character not in "0123456789abcdef" for character in value)
            ):
                raise RuntimeError(f"{key} is not a lowercase SHA-256 digest")
        files = model["files"]
        if not isinstance(files, list) or not files:
            raise RuntimeError("model lock has no files")
        normalized = []
        for item in files:
            if not isinstance(item, dict) or set(item) != {"path", "size", "sha256"}:
                raise RuntimeError("model file entry differs from the closed contract")
            path = PurePosixPath(item["path"])
            if path.is_absolute() or any(part in {"", ".", ".."} for part in path.parts):
                raise RuntimeError(f"unsafe model file path: {item['path']}")
            if not isinstance(item["size"], int) or item["size"] < 0:
                raise RuntimeError(f"invalid model file size: {item['path']}")
            digest = item["sha256"]
            if (
                not isinstance(digest, str)
                or len(digest) != 64
                or any(character not in "0123456789abcdef" for character in digest)
            ):
                raise RuntimeError(f"invalid model file digest: {item['path']}")
            normalized.append(
                {"path": path.as_posix(), "bytes": item["size"], "sha256": digest}
            )
        if [item["path"] for item in normalized] != sorted(
            {item["path"] for item in normalized}
        ):
            raise RuntimeError("model files must be unique and sorted")
        if sum(item["bytes"] for item in normalized) != model["total_size_bytes"]:
            raise RuntimeError("total artifact size differs from the model lock")
        if hashlib.sha256(canonical_bytes(normalized)).hexdigest() != model["content_digest"]:
            raise RuntimeError("content digest differs from the model lock")
        return model


    def expected_receipt(model: dict[str, object]) -> dict[str, object]:
        return {
            "schema": RECEIPT_SCHEMA,
            "model_id": model["id"],
            "repository": model["repo_id"],
            "revision": model["revision"],
            "artifact_manifest_digest": model["artifact_manifest_digest"],
            "content_digest": model["content_digest"],
            "file_count": len(model["files"]),
            "total_size_bytes": model["total_size_bytes"],
            "payload_relative_path": "payload",
            "verification": "full-file-sha256-before-atomic-publication",
        }


    def safe_sha256(path: Path) -> str:
        flags = os.O_RDONLY | getattr(os, "O_CLOEXEC", 0) | getattr(os, "O_NOFOLLOW", 0)
        descriptor = os.open(path, flags)
        try:
            info = os.fstat(descriptor)
            if not stat.S_ISREG(info.st_mode):
                raise RuntimeError(f"model entry is not a regular file: {path}")
            digest = hashlib.sha256()
            with os.fdopen(descriptor, "rb", closefd=False) as stream:
                for chunk in iter(lambda: stream.read(8 * 1024 * 1024), b""):
                    digest.update(chunk)
            return digest.hexdigest()
        finally:
            os.close(descriptor)


    def materialize_regular_files(
        source_root: Path,
        destination: Path,
        files: list[dict[str, object]],
        *,
        allowed_root: Path,
    ) -> None:
        allowed = allowed_root.resolve()
        for item in files:
            source = (source_root / item["path"]).resolve(strict=True)
            try:
                source.relative_to(allowed)
            except ValueError:
                raise RuntimeError(f"model source escaped its cache root: {item['path']}") from None
            info = source.stat()
            if not stat.S_ISREG(info.st_mode) or info.st_size != item["size"]:
                raise RuntimeError(f"model source is not the expected regular file: {item['path']}")
            target = destination / item["path"]
            target.parent.mkdir(parents=True, exist_ok=True, mode=0o750)
            try:
                os.link(source, target)
            except OSError as error:
                if error.errno not in {
                    errno.EXDEV,
                    errno.EPERM,
                    errno.EOPNOTSUPP,
                    errno.ENOTSUP,
                }:
                    raise
                # Copy only when the source and publication path cannot share
                # a regular-file hardlink on the selected storage backend.
                shutil.copyfile(source, target)


    def lightweight_inventory(payload: Path, files: list[dict[str, object]]) -> bool:
        if payload.is_symlink() or not payload.is_dir():
            return False
        for item in files:
            candidate = payload / item["path"]
            try:
                info = candidate.lstat()
            except FileNotFoundError:
                return False
            if not stat.S_ISREG(info.st_mode) or info.st_size != item["size"]:
                return False
        return True


    def receipt_matches(
        receipt_path: Path,
        payload: Path,
        model: dict[str, object],
    ) -> bool:
        try:
            info = receipt_path.lstat()
            if not stat.S_ISREG(info.st_mode) or info.st_size > 16384:
                return False
            receipt = json.loads(receipt_path.read_text(encoding="utf-8"))
        except (FileNotFoundError, OSError, UnicodeDecodeError, json.JSONDecodeError):
            return False
        return receipt == expected_receipt(model) and lightweight_inventory(
            payload, model["files"]
        )


    def verify_payload(payload: Path, model: dict[str, object], workers: int) -> None:
        actual = []
        for candidate in sorted(payload.rglob("*")):
            relative = candidate.relative_to(payload).as_posix()
            info = candidate.lstat()
            if stat.S_ISDIR(info.st_mode):
                continue
            if not stat.S_ISREG(info.st_mode):
                raise RuntimeError(f"model tree contains a non-regular entry: {relative}")
            actual.append(relative)
        expected = [item["path"] for item in model["files"]]
        if actual != expected:
            raise RuntimeError(
                f"inventory mismatch: missing={sorted(set(expected)-set(actual))} "
                f"unexpected={sorted(set(actual)-set(expected))}"
            )

        def verify_one(item: dict[str, object]) -> None:
            candidate = payload / item["path"]
            if candidate.stat().st_size != item["size"] or safe_sha256(candidate) != item["sha256"]:
                raise RuntimeError(f"artifact mismatch: {item['path']}")

        with ThreadPoolExecutor(max_workers=workers) as executor:
            list(executor.map(verify_one, model["files"]))


    def acquire_lock(descriptor: int) -> None:
        deadline = time.monotonic() + int(os.getenv("FS2_CACHE_LOCK_TIMEOUT_SECONDS", "3600"))
        while True:
            try:
                fcntl.flock(descriptor, fcntl.LOCK_EX | fcntl.LOCK_NB)
                return
            except BlockingIOError:
                if time.monotonic() >= deadline:
                    raise RuntimeError("timed out waiting for the shared model-cache writer")
                time.sleep(0.5)


    def resolve_huggingface_snapshot(
        model: dict[str, object], workers: int
    ) -> tuple[Path, str]:
        arguments = {
            "repo_id": model["repo_id"],
            "revision": model["revision"],
            "cache_dir": HF_CACHE_ROOT,
            "allow_patterns": [item["path"] for item in model["files"]],
            "max_workers": workers,
            "token": False,
        }
        try:
            return Path(snapshot_download(**arguments, local_files_only=True)), "huggingface-local-cache"
        except Exception:
            return Path(snapshot_download(**arguments)), "huggingface-download"


    def main() -> int:
        started = time.monotonic()
        model = load_model()
        workers = min(32, max(1, int(os.getenv("FS2_LOCALIZATION_WORKERS", "16"))))
        model_root = CACHE_ROOT / model["id"]
        content_root = model_root / "sha256" / model["content_digest"]
        payload = content_root / "payload"
        receipt_path = content_root / "localization-receipt.json"
        if receipt_matches(receipt_path, payload, model):
            print(json.dumps({"outcome": "cache-hit", "elapsed_seconds": time.monotonic() - started}))
            return 0

        lock_root = model_root / ".locks"
        lock_root.mkdir(parents=True, exist_ok=True, mode=0o750)
        lock_path = lock_root / f"{model['content_digest']}.lock"
        descriptor = os.open(
            lock_path,
            os.O_RDWR | os.O_CREAT | getattr(os, "O_CLOEXEC", 0) | getattr(os, "O_NOFOLLOW", 0),
            0o640,
        )
        try:
            acquire_lock(descriptor)
            if receipt_matches(receipt_path, payload, model):
                print(json.dumps({"outcome": "cache-hit-after-wait", "elapsed_seconds": time.monotonic() - started}))
                return 0
            if content_root.exists():
                raise RuntimeError(
                    "content address exists without its exact immutable receipt; refusing to mutate it"
                )
            staging_parent = model_root / ".staging"
            staging_parent.mkdir(parents=True, exist_ok=True, mode=0o750)
            staging = staging_parent / f"{model['content_digest']}-{uuid.uuid4()}"
            staging_payload = staging / "payload"
            staging_payload.mkdir(parents=True, mode=0o750)
            try:
                source_kind = "huggingface"
                legacy_text = os.getenv("FS2_LEGACY_PAYLOAD_ROOT")
                legacy = Path(legacy_text) if legacy_text else None
                legacy_ready = legacy is not None and lightweight_inventory(
                    legacy, model["files"]
                )
                if legacy_ready:
                    try:
                        materialize_regular_files(
                            legacy,
                            staging_payload,
                            model["files"],
                            allowed_root=legacy,
                        )
                        verify_payload(staging_payload, model, workers)
                        source_kind = "verified-legacy-payload"
                    except Exception:
                        shutil.rmtree(staging_payload)
                        staging_payload.mkdir(mode=0o750)
                        legacy_ready = False
                if not legacy_ready:
                    returned, source_kind = resolve_huggingface_snapshot(model, workers)
                    materialize_regular_files(
                        returned,
                        staging_payload,
                        model["files"],
                        allowed_root=HF_CACHE_ROOT,
                    )
                    verify_payload(staging_payload, model, workers)
                receipt_tmp = staging / ".localization-receipt.json.tmp"
                with receipt_tmp.open("wb") as stream:
                    stream.write(canonical_bytes(expected_receipt(model)))
                    stream.flush()
                    os.fsync(stream.fileno())
                os.replace(receipt_tmp, staging / "localization-receipt.json")
                content_root.parent.mkdir(parents=True, exist_ok=True, mode=0o750)
                os.replace(staging, content_root)
            except Exception:
                if staging.exists():
                    shutil.rmtree(staging)
                raise
        finally:
            os.close(descriptor)
        print(
            json.dumps(
                {
                    "outcome": "localized",
                    "source": source_kind,
                    "elapsed_seconds": time.monotonic() - started,
                }
            )
        )
        return 0


    if __name__ == "__main__":
        try:
            raise SystemExit(main())
        except Exception as error:
            print(f"localization failed: {error}", file=sys.stderr, flush=True)
            raise
  model.lock.json: |
    {
      "schema_version": 2,
      "model": {
        "id": "cosmos3-nano",
        "repo_id": "nvidia/Cosmos3-Nano",
        "revision": "7a312c868bcce8e40b3eb40861300a9d0ba3fde1",
        "artifact_manifest_digest": "49b4bf03d84857892b86366ef2f6c3b68ac575cb1a3f2de4122c41e638f4cc6e",
        "content_digest": "dfa7b03382ba78d7f80703652706c3cfa777cefac48634df49345c4302af2c95",
        "total_size_bytes": 34986890561,
        "files": [
          {
            "path": ".gitattributes",
            "size": 2357,
            "sha256": "dbb830a9fae52f8eb8b7ccdaf1e1371959be408c722cd5453440d0690fd1d759"
          },
          {
            "path": "BIAS.md",
            "size": 4720,
            "sha256": "1d65943c20dac47c4250b5a4a71266c001f6c85a17dad556ad00dbfc2a8115a9"
          },
          {
            "path": "EXPLAINABILITY.md",
            "size": 3189,
            "sha256": "7d9e73df359fc9c0fcffdc18045018f55f124e5a6b6f71268a93d63c56e1a997"
          },
          {
            "path": "PRIVACY.md",
            "size": 1215,
            "sha256": "0298f236e550d4064cd8d83babad8dfc791295bba602210ca6c8106a993ca699"
          },
          {
            "path": "README.md",
            "size": 44048,
            "sha256": "70cd946ba24ef287f5519dc76b9f3aa2e5dcac7e18560756645364f992757a4e"
          },
          {
            "path": "SAFETY.md",
            "size": 3677,
            "sha256": "b59cf989cb66eb3b1c5e0692d04c42b9a39751801259b225733ab9493e08f03b"
          },
          {
            "path": "assets/example_action_fd_agibotworld_4chunk_output.mp4",
            "size": 672918,
            "sha256": "9b01266b6cd27478514133b00ada9c33db3f9444167f09942c11f880c629c8c0"
          },
          {
            "path": "assets/example_action_fd_agibotworld_action_chunks.json",
            "size": 48386,
            "sha256": "ba8408f727f9c77d4450b239069f81e9cdd9d099bf05da7f33f5bfb4cb2d55cd"
          },
          {
            "path": "assets/example_action_fd_agibotworld_first_frame.png",
            "size": 637580,
            "sha256": "78b7288846b05c2265f4aa9a31b7aa905d75e0154c2126adcf8472432a81053c"
          },
          {
            "path": "assets/example_action_id_av_0_input.mp4",
            "size": 1125838,
            "sha256": "ff205f86ae169031c0e12c8c6bc8aabe24aeb44b84a16cba977669b29912f5fc"
          },
          {
            "path": "assets/example_action_id_av_0_output.json",
            "size": 15976,
            "sha256": "0c6a6fe8f1bddd1e119a37e66a168b82ef62e4ee99c024370d2d25c80ee83a24"
          },
          {
            "path": "assets/example_action_id_av_0_output.png",
            "size": 135678,
            "sha256": "bc0adf920602d9c1ecd0a2e2c1d370ac0b5af22eaddf7d9e1a75efca268e6823"
          },
          {
            "path": "assets/example_action_id_av_1_input.mp4",
            "size": 1639407,
            "sha256": "169e65cee76ef7c6366987a8e4c8e4ec6f803f5e624ba2ef39ba1f0c672925aa"
          },
          {
            "path": "assets/example_action_id_av_1_output.json",
            "size": 15704,
            "sha256": "5ee587d25fb6e5fe5f54febaa63d7890c029b141f2eed047b1b7ee93877fde3e"
          },
          {
            "path": "assets/example_action_id_av_1_output.png",
            "size": 133968,
            "sha256": "056ff91da285cb32e267d5e2115e7a3f7085bd26dd2d903457082b002fffa769"
          },
          {
            "path": "assets/example_i2v_input.jpg",
            "size": 860765,
            "sha256": "1de51eb5c6d51ec97aa3351a6685717b0253e36073eb9cdd3ef65affa115a4e2"
          },
          {
            "path": "assets/example_i2v_output.mp4",
            "size": 22843643,
            "sha256": "35f776ae09a63f3dc050ef3924c298748ceeaef186107b8901ec10b3da320e89"
          },
          {
            "path": "assets/example_i2v_prompt.json",
            "size": 9176,
            "sha256": "baa77ef7ae92c889a96ff0402db283ff07d9da881d31abfa8b986f8481cb8273"
          },
          {
            "path": "assets/example_i2vs_output.mp4",
            "size": 17843192,
            "sha256": "5ef623a93fe7ef813e3ca3a3037428a0f93a7146595d848ce497cd40d7fa1a2e"
          },
          {
            "path": "assets/example_reasoning_input.png",
            "size": 230656,
            "sha256": "6686b937bdb28e2c9804c435bd63a7ab56ac1531ffc9b482f2b8a75aed8770f5"
          },
          {
            "path": "assets/example_reasoning_prompt.json",
            "size": 151,
            "sha256": "fb6fbd57e5af6b09960d9960c709e1b422faf299a4b34139d212c7fccc488348"
          },
          {
            "path": "assets/example_t2v_diffusers_output.mp4",
            "size": 1541990,
            "sha256": "b630246f9bc9bed4598b5676f4b0b688e27f68f7a10246e233c8a934e699467c"
          },
          {
            "path": "assets/example_t2v_output.mp4",
            "size": 9163599,
            "sha256": "8da069632812e3214a51672257c78c5d345b6694d4877d536a0bb3287269ec88"
          },
          {
            "path": "assets/example_t2v_prompt.json",
            "size": 7710,
            "sha256": "61c9c4b46b6787d967cc509a2bf323766e70bf5ecf40e09a739362beac135677"
          },
          {
            "path": "assets/example_t2v_prompt_short.txt",
            "size": 46,
            "sha256": "cc739831cb4ce9c8de022522c0486943e7c5565978d6fae4f99836be2c380e73"
          },
          {
            "path": "assets/example_t2vs_output.mp4",
            "size": 4053521,
            "sha256": "3cc8ac15acd0a448a1fcbd072fad9d465c0dace760efd8e2be1e9a7b9f89af0c"
          },
          {
            "path": "assets/example_t2vs_prompt.json",
            "size": 8885,
            "sha256": "bb23395fe07806a1af6d1dd57299816924b79f306bc789a05f1fb1176f0ecf67"
          },
          {
            "path": "assets/negative_prompt.json",
            "size": 15379,
            "sha256": "7a60bd0c1335641cddd8b3ae66de767d2282f08861324f0ebe850e613af6a3a1"
          },
          {
            "path": "chat_template.json",
            "size": 5499,
            "sha256": "5c72a170d2a4a1a3bc5adad2e689ae28138a9700e5b8c96c0266331e86c0acce"
          },
          {
            "path": "checkpoint.json",
            "size": 2,
            "sha256": "44136fa355b3678a1146ad16f7e8649e94fb4fc21fe77e8310c060f61caaff8a"
          },
          {
            "path": "config.json",
            "size": 8166,
            "sha256": "c32f2468a54542c21946bc8eab6172b911dcec9a7193a94c023ea2d4073bcda6"
          },
          {
            "path": "generation_config.json",
            "size": 269,
            "sha256": "8469742d1fce0de951c8909b26a2c0c0d8490837ce476efb114da9e0cefc4d44"
          },
          {
            "path": "images/benchmark-action-1.png",
            "size": 101013,
            "sha256": "11772abc8515d29e62403d12a19194308e513d323852de9c7878a1006a3660ee"
          },
          {
            "path": "images/benchmark-overall.png",
            "size": 268890,
            "sha256": "25425f6be1f1f2099c484f3e887b420d18728de1f79e4600770461ef94ed44c7"
          },
          {
            "path": "images/benchmark-reasoning.png",
            "size": 2683196,
            "sha256": "ba95a3b243fc23c644e12dd8b22794af7002a1ee5a469322dbe1a55f080a12b4"
          },
          {
            "path": "images/benchmark-visual-audio.png",
            "size": 366106,
            "sha256": "2faab808cd9871de94c099ccc4bca428206814d818224879726d54e5a80b2578"
          },
          {
            "path": "merges.txt",
            "size": 1671839,
            "sha256": "599bab54075088774b1733fde865d5bd747cbcc7a547c5bc12610e874e26f5e3"
          },
          {
            "path": "model.safetensors.index.json",
            "size": 112969,
            "sha256": "43bdea0b60472d7b4f74f280d5eb508ec5e895f24287af8d2ecf2d0e2738708f"
          },
          {
            "path": "model_index.json",
            "size": 502,
            "sha256": "90088a6638deb68418d795971ad48e6d5aa3f7764d69f3c62bbca9e8f3485bcd"
          },
          {
            "path": "modular_model_index.json",
            "size": 1667,
            "sha256": "849634afcffb6958d8ed71e367a1d658b092c660e8ee97b65aa81458dbf54d33"
          },
          {
            "path": "preprocessor_config.json",
            "size": 390,
            "sha256": "27225450ac9c6529872ee1924fcb0962ff5634834f817040f444118116f4e516"
          },
          {
            "path": "scheduler/scheduler_config.json",
            "size": 884,
            "sha256": "3e25a6a5b472e77ba45cb17a4956e70ff922b996e4f9bf421f56e7b19d1359ec"
          },
          {
            "path": "sound_tokenizer/config.json",
            "size": 1310,
            "sha256": "a8194162a0126753981aa81054ab14382f01776fcc56e96cde4867a87bf4e8da"
          },
          {
            "path": "sound_tokenizer/diffusion_pytorch_model.safetensors",
            "size": 1985176840,
            "sha256": "a4b6da5975bf89f6853fe589ad4752d281ac79fbdfad52ea90537fa080b4b9c2"
          },
          {
            "path": "text_tokenizer/added_tokens.json",
            "size": 707,
            "sha256": "c0284b582e14987fbd3d5a2cb2bd139084371ed9acbae488829a1c900833c680"
          },
          {
            "path": "text_tokenizer/chat_template.jinja",
            "size": 5292,
            "sha256": "3636d0f0bd6bef02654cdffdc447b79cb2cef8ab02cc75267345946291a489e4"
          },
          {
            "path": "text_tokenizer/merges.txt",
            "size": 1671853,
            "sha256": "8831e4f1a044471340f7c0a83d7bd71306a5b867e95fd870f74d0c5308a904d5"
          },
          {
            "path": "text_tokenizer/special_tokens_map.json",
            "size": 613,
            "sha256": "76862e765266b85aa9459767e33cbaf13970f327a0e88d1c65846c2ddd3a1ecd"
          },
          {
            "path": "text_tokenizer/tokenizer.json",
            "size": 11422654,
            "sha256": "aeb13307a71acd8fe81861d94ad54ab689df773318809eed3cbe794b4492dae4"
          },
          {
            "path": "text_tokenizer/tokenizer_config.json",
            "size": 5404,
            "sha256": "355f2b4e5bad7b01f11ef6cb68ebc176f61b95c3276092ea225b1bea0e01e95c"
          },
          {
            "path": "text_tokenizer/vocab.json",
            "size": 2776833,
            "sha256": "ca10d7e9fb3ed18575dd1e277a2579c16d108e32f27439684afa0e10b1440910"
          },
          {
            "path": "tokenizer.json",
            "size": 7032403,
            "sha256": "a5d85b6dcc535e6b93115a9ef287e6132fdbf30270da6218194ba742261173c7"
          },
          {
            "path": "tokenizer_config.json",
            "size": 10868,
            "sha256": "c2da771801886ad9ae98181793ffd3dfb7f1af30f6f7c6a4e15d7dbba52e2399"
          },
          {
            "path": "transformer/config.json",
            "size": 1383,
            "sha256": "282862cf9c42a257dbd2f00e41b5ef65c3aacfb693ad1f8ab28b05a02e1b9ffe"
          },
          {
            "path": "transformer/diffusion_pytorch_model-00001-of-00007.safetensors",
            "size": 4902257280,
            "sha256": "7d0f718198cac14cf323d58568c78aa1290b4bd1dcced6481624b5b9c6d70dc2"
          },
          {
            "path": "transformer/diffusion_pytorch_model-00002-of-00007.safetensors",
            "size": 4999863656,
            "sha256": "7e6e7d270c193a58a1aec0cbc89f6f5afcaef6e3b951d86b0a432244c449e34e"
          },
          {
            "path": "transformer/diffusion_pytorch_model-00003-of-00007.safetensors",
            "size": 4932719448,
            "sha256": "15d08936149a7e77c6668e5b1cd47dc92a35e33741f0090d619045b66da9ff8d"
          },
          {
            "path": "transformer/diffusion_pytorch_model-00004-of-00007.safetensors",
            "size": 4983084656,
            "sha256": "02cebbb557bced701eb825901e1de5f86d2e1feb49cb9a8d1fb7d1b878fdb31b"
          },
          {
            "path": "transformer/diffusion_pytorch_model-00005-of-00007.safetensors",
            "size": 4949498576,
            "sha256": "a5e3edd742b8f1fcf27a3eb3878c67dfcb0e6881588fa35deef503d640ea69d1"
          },
          {
            "path": "transformer/diffusion_pytorch_model-00006-of-00007.safetensors",
            "size": 4261644464,
            "sha256": "1bb99750f7ab4a47f8b0403a87aa43fefc9648660609ab2ace4cab361b957cd0"
          },
          {
            "path": "transformer/diffusion_pytorch_model-00007-of-00007.safetensors",
            "size": 1318361208,
            "sha256": "3f159bc8b82477adde771ae9e4f54f76564e02844a2fc6267903bd745111d7f6"
          },
          {
            "path": "transformer/diffusion_pytorch_model.safetensors.index.json",
            "size": 78854,
            "sha256": "e664881df3452d3aadbc7fd36075871077397dfa21ab8b8818143fab99fb9cb0"
          },
          {
            "path": "vae/config.json",
            "size": 1750,
            "sha256": "1d3312edfb30b8486720ea29f3532f89c5b5fc05cc571d7664c6f5162effc23e"
          },
          {
            "path": "vae/diffusion_pytorch_model.safetensors",
            "size": 1409400600,
            "sha256": "230496cb59ff85bc9c040487737c4062480cb61c71e697b197b4c30142f2a0da"
          },
          {
            "path": "video_preprocessor_config.json",
            "size": 385,
            "sha256": "7768af27c1fafa9cc9011c1dc20067e03f8915e03b63504550e11d5066986d13"
          },
          {
            "path": "vision_encoder/config.json",
            "size": 509,
            "sha256": "6b8457bba07f3ec61199a47e88f2ec3d9af57247b86e69cd7464b70307f069f4"
          },
          {
            "path": "vision_encoder/model.safetensors",
            "size": 1152811416,
            "sha256": "b0c0f719fcf5f2cdadb676701477c236020713657976592ad046583707a6a95a"
          },
          {
            "path": "vocab.json",
            "size": 2776833,
            "sha256": "ca10d7e9fb3ed18575dd1e277a2579c16d108e32f27439684afa0e10b1440910"
          }
        ]
      }
    }
  adapter.py: |
    from __future__ import annotations

    import asyncio
    import base64
    import binascii
    import hashlib
    import json
    import os
    import struct
    import time
    import uuid
    from contextlib import asynccontextmanager
    from typing import Annotated, Literal, Union

    import httpx
    import uvicorn
    from fastapi import FastAPI, Request
    from fastapi.responses import JSONResponse, PlainTextResponse
    from pydantic import BaseModel, ConfigDict, Field, model_validator


    MODEL_REPOSITORY = "nvidia/Cosmos3-Nano"
    MODEL_REVISION = "7a312c868bcce8e40b3eb40861300a9d0ba3fde1"
    RESOLVED_MODEL = f"{MODEL_REPOSITORY}@{MODEL_REVISION}"
    UPSTREAM_BASE_URL = "http://127.0.0.1:8000"
    MAX_RAW_BYTES = 64 * 1024 * 1024
    MAX_ENCODED_BYTES = 90 * 1024 * 1024
    MAX_IMAGE_RESPONSE_BYTES = MAX_ENCODED_BYTES + (1024 * 1024)
    BACKEND_ID = os.getenv("POD_NAME", "unknown")

    RESULTS = {
        "accepted": 0,
        "completed": 0,
        "failed": 0,
        "rejected": 0,
        "timed_out": 0,
        "cancelled": 0,
    }
    DURATIONS = {
        "queue": {"count": 0, "sum": 0.0},
        "upstream": {"count": 0, "sum": 0.0},
        "encoding": {"count": 0, "sum": 0.0},
        "total": {"count": 0, "sum": 0.0},
    }
    OUTPUT_BYTES = 0
    OUTPUT_FRAMES = 0
    READY = 0


    class CommonRequest(BaseModel):
        model_config = ConfigDict(extra="forbid", strict=True)

        model: Literal[RESOLVED_MODEL] = RESOLVED_MODEL
        prompt: str = Field(min_length=1, max_length=4096)
        negative_prompt: str = Field(default="", max_length=4096)
        seed: int = Field(default=0, ge=0, le=4294967295)
        num_inference_steps: int = Field(default=30, ge=1, le=50)
        guidance_scale: float = Field(default=7.0, ge=0.0, le=20.0)

        @staticmethod
        def validate_size(size: str) -> None:
            try:
                width_text, height_text = size.split("x", 1)
                width, height = int(width_text), int(height_text)
            except (AttributeError, TypeError, ValueError):
                raise ValueError("size must use WIDTHxHEIGHT") from None
            if not 256 <= width <= 1280 or not 256 <= height <= 720:
                raise ValueError("size must be between 256x256 and 1280x720")
            if width % 16 or height % 16:
                raise ValueError("width and height must be multiples of 16")
            if width * height > 1280 * 720:
                raise ValueError("width times height must not exceed 921600 pixels")


    class TextToImageRequest(CommonRequest):
        mode: Literal["text-to-image"]
        size: str = Field(default="512x512", min_length=7, max_length=9)
        output_format: Literal["png"] = "png"

        @model_validator(mode="after")
        def dimensions_are_bounded(self) -> "TextToImageRequest":
            self.validate_size(self.size)
            return self


    class TextToVideoRequest(CommonRequest):
        mode: Literal["text-to-video"]
        size: str = Field(default="448x256", min_length=7, max_length=9)
        num_frames: int = Field(default=25, ge=5, le=400)
        fps: int = Field(default=24, ge=1, le=30)
        output_format: Literal["mp4"] = "mp4"

        @model_validator(mode="after")
        def dimensions_are_bounded(self) -> "TextToVideoRequest":
            self.validate_size(self.size)
            return self


    GenerateRequest = Annotated[
        Union[TextToImageRequest, TextToVideoRequest],
        Field(discriminator="mode"),
    ]


    class AdapterError(Exception):
        def __init__(
            self,
            status_code: int,
            code: str,
            message: str,
            retryable: bool,
        ) -> None:
            super().__init__(message)
            self.status_code = status_code
            self.code = code
            self.message = message
            self.retryable = retryable


    class AdmissionRejected(Exception):
        pass


    class AdmissionController:
        """Allow one active generation and at most one queued generation."""

        def __init__(self) -> None:
            self._semaphore = asyncio.Semaphore(1)
            self._lock = asyncio.Lock()
            self._outstanding = 0
            self._active = 0

        async def enter(self) -> float:
            queued_at = time.monotonic()
            async with self._lock:
                if self._outstanding >= 2:
                    raise AdmissionRejected
                self._outstanding += 1
            try:
                await self._semaphore.acquire()
            except BaseException:
                async with self._lock:
                    self._outstanding -= 1
                raise
            async with self._lock:
                self._active += 1
            return time.monotonic() - queued_at

        async def leave(self) -> None:
            async with self._lock:
                self._active -= 1
                self._outstanding -= 1
            self._semaphore.release()

        async def snapshot(self) -> tuple[int, int]:
            async with self._lock:
                return self._active, self._outstanding - self._active


    ADMISSION = AdmissionController()


    @asynccontextmanager
    async def lifespan(app: FastAPI):
        timeout = httpx.Timeout(1800.0, connect=5.0, pool=5.0)
        limits = httpx.Limits(max_connections=2, max_keepalive_connections=2)
        app.state.client = httpx.AsyncClient(timeout=timeout, limits=limits)
        try:
            yield
        finally:
            await app.state.client.aclose()


    app = FastAPI(
        title="Cosmos3-Nano bounded media adapter",
        docs_url=None,
        redoc_url=None,
        openapi_url=None,
        lifespan=lifespan,
    )


    def error_response(
        request_id: str,
        status_code: int,
        code: str,
        message: str,
        retryable: bool,
        *,
        retry_after: str | None = None,
    ) -> JSONResponse:
        headers = {
            "X-Backend-Id": BACKEND_ID,
            "X-Request-Id": request_id,
        }
        if retry_after is not None:
            headers["Retry-After"] = retry_after
        return JSONResponse(
            status_code=status_code,
            headers=headers,
            content={
                "error": {
                    "request_id": request_id,
                    "code": code,
                    "message": message,
                    "retryable": retryable,
                }
            },
        )


    async def read_limited(response: httpx.Response, limit: int) -> bytes:
        if response.status_code >= 400:
            raise AdapterError(
                502,
                "upstream_error",
                f"model runtime returned HTTP {response.status_code}",
                response.status_code >= 500,
            )
        content_length = response.headers.get("content-length")
        if content_length is not None:
            try:
                if int(content_length) > limit:
                    raise AdapterError(
                        502,
                        "upstream_output_too_large",
                        "model runtime output exceeds the inline response limit",
                        False,
                    )
            except ValueError:
                raise AdapterError(
                    502,
                    "invalid_upstream_response",
                    "model runtime returned an invalid content length",
                    False,
                ) from None
        result = bytearray()
        async for chunk in response.aiter_bytes():
            result.extend(chunk)
            if len(result) > limit:
                raise AdapterError(
                    502,
                    "upstream_output_too_large",
                    "model runtime output exceeds the inline response limit",
                    False,
                )
        return bytes(result)


    def validate_png(raw: bytes, expected_width: int, expected_height: int) -> None:
        if len(raw) < 24 or raw[:8] != b"\x89PNG\r\n\x1a\n" or raw[12:16] != b"IHDR":
            raise AdapterError(
                502,
                "invalid_upstream_media",
                "model runtime did not return a valid PNG image",
                False,
            )
        width, height = struct.unpack(">II", raw[16:24])
        if (width, height) != (expected_width, expected_height):
            raise AdapterError(
                502,
                "invalid_upstream_media",
                "model runtime returned unexpected image dimensions",
                False,
            )


    async def generate_image(
        client: httpx.AsyncClient,
        body: TextToImageRequest,
        width: int,
        height: int,
    ) -> tuple[bytes, str]:
        payload = {
            "model": MODEL_REPOSITORY,
            "prompt": body.prompt,
            "negative_prompt": body.negative_prompt,
            "n": 1,
            "size": body.size,
            "response_format": "b64_json",
            "seed": body.seed,
            "num_inference_steps": body.num_inference_steps,
            "guidance_scale": body.guidance_scale,
        }
        async with client.stream(
            "POST",
            f"{UPSTREAM_BASE_URL}/v1/images/generations",
            json=payload,
        ) as response:
            encoded_response = await read_limited(response, MAX_IMAGE_RESPONSE_BYTES)
        try:
            response_payload = json.loads(encoded_response)
            encoded_media = response_payload["data"][0]["b64_json"]
        except (KeyError, IndexError, TypeError, UnicodeDecodeError, json.JSONDecodeError):
            raise AdapterError(
                502,
                "invalid_upstream_response",
                "model runtime returned an invalid image response",
                False,
            ) from None
        if not isinstance(encoded_media, str) or len(encoded_media.encode("ascii")) > MAX_ENCODED_BYTES:
            raise AdapterError(
                502,
                "upstream_output_too_large",
                "model runtime image exceeds the inline response limit",
                False,
            )
        try:
            raw = base64.b64decode(encoded_media, validate=True)
        except (ValueError, UnicodeEncodeError, binascii.Error):
            raise AdapterError(
                502,
                "invalid_upstream_media",
                "model runtime returned invalid base64 image data",
                False,
            ) from None
        if len(raw) > MAX_RAW_BYTES:
            raise AdapterError(
                502,
                "upstream_output_too_large",
                "model runtime image exceeds the inline response limit",
                False,
            )
        validate_png(raw, width, height)
        return raw, "image/png"


    async def generate_video(
        client: httpx.AsyncClient,
        body: TextToVideoRequest,
    ) -> tuple[bytes, str]:
        fields = {
            "model": MODEL_REPOSITORY,
            "prompt": body.prompt,
            "negative_prompt": body.negative_prompt,
            "size": body.size,
            "num_frames": str(body.num_frames),
            "fps": str(body.fps),
            "seed": str(body.seed),
            "num_inference_steps": str(body.num_inference_steps),
            "guidance_scale": str(body.guidance_scale),
            "max_sequence_length": "4096",
            "flow_shift": "10.0",
            "extra_params": json.dumps(
                {
                    "use_resolution_template": False,
                    "use_duration_template": False,
                    "guardrails": False,
                },
                separators=(",", ":"),
            ),
        }
        multipart = {name: (None, value) for name, value in fields.items()}
        async with client.stream(
            "POST",
            f"{UPSTREAM_BASE_URL}/v1/videos/sync",
            files=multipart,
            headers={"Accept": "video/mp4"},
        ) as response:
            content_type = response.headers.get("content-type", "").split(";", 1)[0].strip().lower()
            if response.status_code < 400 and content_type not in {"video/mp4", "application/mp4"}:
                raise AdapterError(
                    502,
                    "invalid_upstream_media",
                    "model runtime returned an unexpected video content type",
                    False,
                )
            raw = await read_limited(response, MAX_RAW_BYTES)
        if len(raw) < 16 or b"ftyp" not in raw[:32]:
            raise AdapterError(
                502,
                "invalid_upstream_media",
                "model runtime did not return a valid MP4 container",
                False,
            )
        return raw, "video/mp4"


    @app.get("/healthz")
    async def healthz() -> dict[str, str]:
        return {"status": "ok"}


    @app.get("/readyz")
    async def readyz(request: Request):
        global READY
        try:
            response = await request.app.state.client.get(f"{UPSTREAM_BASE_URL}/health")
            if response.status_code >= 400:
                raise RuntimeError("not ready")
        except (httpx.HTTPError, RuntimeError):
            READY = 0
            return JSONResponse(status_code=503, content={"status": "not-ready"})
        READY = 1
        return {"status": "ready"}


    @app.get("/metrics", response_class=PlainTextResponse)
    async def metrics() -> PlainTextResponse:
        active, queued = await ADMISSION.snapshot()
        lines = [
            "# HELP fs2_cosmos_adapter_requests_total Requests by fixed result class.",
            "# TYPE fs2_cosmos_adapter_requests_total counter",
        ]
        for result in ("accepted", "completed", "failed", "rejected", "timed_out", "cancelled"):
            lines.append(
                f'fs2_cosmos_adapter_requests_total{{result="{result}"}} {RESULTS[result]}'
            )
        lines.extend(
            [
                "# HELP fs2_cosmos_adapter_active_requests Active model generations.",
                "# TYPE fs2_cosmos_adapter_active_requests gauge",
                f"fs2_cosmos_adapter_active_requests {active}",
                "# HELP fs2_cosmos_adapter_queue_depth Accepted requests waiting for the GPU.",
                "# TYPE fs2_cosmos_adapter_queue_depth gauge",
                f"fs2_cosmos_adapter_queue_depth {queued}",
                "# HELP fs2_cosmos_adapter_ready Whether the upstream model is ready.",
                "# TYPE fs2_cosmos_adapter_ready gauge",
                f"fs2_cosmos_adapter_ready {READY}",
                "# HELP fs2_cosmos_adapter_duration_seconds Generation durations by fixed phase.",
                "# TYPE fs2_cosmos_adapter_duration_seconds summary",
            ]
        )
        for phase in ("queue", "upstream", "encoding", "total"):
            lines.append(
                f'fs2_cosmos_adapter_duration_seconds_count{{phase="{phase}"}} '
                f'{DURATIONS[phase]["count"]}'
            )
            lines.append(
                f'fs2_cosmos_adapter_duration_seconds_sum{{phase="{phase}"}} '
                f'{DURATIONS[phase]["sum"]:.9f}'
            )
        lines.extend(
            [
                "# HELP fs2_cosmos_adapter_output_bytes_total Valid output bytes returned.",
                "# TYPE fs2_cosmos_adapter_output_bytes_total counter",
                f"fs2_cosmos_adapter_output_bytes_total {OUTPUT_BYTES}",
                "# HELP fs2_cosmos_adapter_output_frames_total Valid output frames returned.",
                "# TYPE fs2_cosmos_adapter_output_frames_total counter",
                f"fs2_cosmos_adapter_output_frames_total {OUTPUT_FRAMES}",
            ]
        )
        return PlainTextResponse("\n".join(lines) + "\n", media_type="text/plain; version=0.0.4")


    @app.post("/generate")
    async def generate(body: GenerateRequest, request: Request) -> JSONResponse:
        global OUTPUT_BYTES, OUTPUT_FRAMES
        request_id = str(uuid.uuid4())
        total_started = time.monotonic()
        try:
            queue_seconds = await ADMISSION.enter()
        except AdmissionRejected:
            RESULTS["rejected"] += 1
            return error_response(
                request_id,
                429,
                "queue_full",
                "one generation is active and one is already queued",
                True,
                retry_after="5",
            )

        RESULTS["accepted"] += 1
        DURATIONS["queue"]["count"] += 1
        DURATIONS["queue"]["sum"] += queue_seconds
        upstream_started = time.monotonic()
        try:
            width_text, height_text = body.size.split("x", 1)
            width, height = int(width_text), int(height_text)
            if isinstance(body, TextToImageRequest):
                raw, mime_type = await generate_image(
                    request.app.state.client,
                    body,
                    width,
                    height,
                )
                frames = 1
                fps = None
            else:
                raw, mime_type = await generate_video(request.app.state.client, body)
                frames = body.num_frames
                fps = body.fps
            upstream_seconds = time.monotonic() - upstream_started
            encoding_started = time.monotonic()
            encoded = base64.b64encode(raw).decode("ascii")
            encoding_seconds = time.monotonic() - encoding_started
            if len(encoded.encode("ascii")) > MAX_ENCODED_BYTES:
                raise AdapterError(
                    502,
                    "encoded_output_too_large",
                    "encoded media exceeds the inline response limit",
                    False,
                )
            total_seconds = time.monotonic() - total_started
            DURATIONS["upstream"]["count"] += 1
            DURATIONS["upstream"]["sum"] += upstream_seconds
            DURATIONS["encoding"]["count"] += 1
            DURATIONS["encoding"]["sum"] += encoding_seconds
            DURATIONS["total"]["count"] += 1
            DURATIONS["total"]["sum"] += total_seconds
            RESULTS["completed"] += 1
            OUTPUT_BYTES += len(raw)
            OUTPUT_FRAMES += frames
            content = {
                "model": MODEL_REPOSITORY,
                "revision": MODEL_REVISION,
                "mode": body.mode,
                "mime_type": mime_type,
                "data_base64": encoded,
                "bytes": len(raw),
                "sha256": hashlib.sha256(raw).hexdigest(),
                "width": width,
                "height": height,
                "frames": frames,
                "fps": fps,
                "timings_ms": {
                    "queue": queue_seconds * 1000.0,
                    "upstream": upstream_seconds * 1000.0,
                    "total": total_seconds * 1000.0,
                },
            }
            return JSONResponse(
                status_code=200,
                headers={
                    "X-Backend-Id": BACKEND_ID,
                    "X-Request-Id": request_id,
                },
                content=content,
            )
        except AdapterError as exc:
            RESULTS["failed"] += 1
            return error_response(
                request_id,
                exc.status_code,
                exc.code,
                exc.message,
                exc.retryable,
            )
        except httpx.TimeoutException:
            RESULTS["timed_out"] += 1
            return error_response(
                request_id,
                504,
                "upstream_timeout",
                "model runtime did not finish before the request deadline",
                True,
            )
        except httpx.HTTPError:
            RESULTS["failed"] += 1
            return error_response(
                request_id,
                502,
                "upstream_unavailable",
                "model runtime is unavailable",
                True,
            )
        except asyncio.CancelledError:
            RESULTS["cancelled"] += 1
            raise
        except Exception:
            RESULTS["failed"] += 1
            return error_response(
                request_id,
                500,
                "adapter_error",
                "the media adapter failed to complete the request",
                False,
            )
        finally:
            await ADMISSION.leave()


    if __name__ == "__main__":
        uvicorn.run(app, host="0.0.0.0", port=8080, workers=1, log_level="info")
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cosmos3-nano
  namespace: fs2-models
  labels:
    app.kubernetes.io/component: model-runtime
    app.kubernetes.io/instance: cosmos3-nano
    app.kubernetes.io/managed-by: fs2-serve-models
    app.kubernetes.io/name: cosmos3-nano
    app.kubernetes.io/part-of: fs2-serve
    app.kubernetes.io/version: 7a312c868bcce8e40b3eb40861300a9d0ba3fde1
  annotations:
    fs2.nebius/cache-content-path: /model-cache/cosmos3-nano/sha256/dfa7b03382ba78d7f80703652706c3cfa777cefac48634df49345c4302af2c95/payload
    fs2.nebius/localization-receipt-schema: fs2-serve.nebius.ai/shared-cache-localization-receipt/v2
    fs2.nebius/model-precision: bfloat16
    fs2.nebius/model-repository: nvidia/Cosmos3-Nano
    fs2.nebius/model-content-digest: sha256:dfa7b03382ba78d7f80703652706c3cfa777cefac48634df49345c4302af2c95
    fs2.nebius/runtime-image-digest: sha256:6d2630c7d637b699557573f2c3fee8df5d4d0cd718977aa22549ed6a6ef30587
spec:
  progressDeadlineSeconds: 2700
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app.kubernetes.io/instance: cosmos3-nano
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: model-runtime
        app.kubernetes.io/instance: cosmos3-nano
        app.kubernetes.io/managed-by: fs2-serve-models
        app.kubernetes.io/name: cosmos3-nano
        app.kubernetes.io/part-of: fs2-serve
        app.kubernetes.io/version: 7a312c868bcce8e40b3eb40861300a9d0ba3fde1
      annotations:
        fs2.nebius/cache-content-path: /model-cache/cosmos3-nano/sha256/dfa7b03382ba78d7f80703652706c3cfa777cefac48634df49345c4302af2c95/payload
        fs2.nebius/localization-receipt-schema: fs2-serve.nebius.ai/shared-cache-localization-receipt/v2
        fs2.nebius/model-precision: bfloat16
        fs2.nebius/model-repository: nvidia/Cosmos3-Nano
        fs2.nebius/model-revision: 7a312c868bcce8e40b3eb40861300a9d0ba3fde1
        fs2.nebius/model-content-digest: sha256:dfa7b03382ba78d7f80703652706c3cfa777cefac48634df49345c4302af2c95
        fs2.nebius/runtime-image-digest: sha256:6d2630c7d637b699557573f2c3fee8df5d4d0cd718977aa22549ed6a6ef30587
    spec:
      automountServiceAccountToken: false
      enableServiceLinks: false
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: cosmos3-nano
      terminationGracePeriodSeconds: 300
      tolerations:
        - effect: NoSchedule
          key: dedicated
          operator: Equal
          value: fs2-inference
      initContainers:
        - name: localize-model
          image: docker.io/vllm/vllm-omni@sha256:6d2630c7d637b699557573f2c3fee8df5d4d0cd718977aa22549ed6a6ef30587
          imagePullPolicy: IfNotPresent
          command:
            - python3
            - /contract/localize.py
          env:
            - name: FS2_CACHE_ROOT
              value: /model-cache
            - name: FS2_LOCALIZATION_WORKERS
              value: "16"
            - name: HF_HUB_DISABLE_TELEMETRY
              value: "1"
            - name: HF_HOME
              value: /model-cache/huggingface
          resources:
            limits:
              cpu: "16"
              ephemeral-storage: 8Gi
              memory: 32Gi
            requests:
              cpu: "4"
              ephemeral-storage: 1Gi
              memory: 8Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - mountPath: /contract
              name: adapter
              readOnly: true
            - mountPath: /model-cache
              name: model-cache
      containers:
        - name: vllm-omni
          image: docker.io/vllm/vllm-omni@sha256:6d2630c7d637b699557573f2c3fee8df5d4d0cd718977aa22549ed6a6ef30587
          imagePullPolicy: IfNotPresent
          command:
            - vllm
          args:
            - serve
            - /model-cache/cosmos3-nano/sha256/dfa7b03382ba78d7f80703652706c3cfa777cefac48634df49345c4302af2c95/payload
            - --omni
            - --served-model-name
            - nvidia/Cosmos3-Nano
            - --revision
            - 7a312c868bcce8e40b3eb40861300a9d0ba3fde1
            - --host
            - 0.0.0.0
            - --port
            - "8000"
            - --init-timeout
            - "1800"
            - --no-guardrails
            - --model-class-name
            - Cosmos3OmniDiffusersPipeline
          env:
            - name: DO_NOT_TRACK
              value: "1"
            - name: FS2_MODEL_PRECISION
              value: bfloat16
            - name: HF_HUB_DISABLE_TELEMETRY
              value: "1"
            - name: HF_HUB_OFFLINE
              value: "1"
            - name: HF_HOME
              value: /runtime-cache/huggingface
            - name: HOME
              value: /runtime-cache/home
            - name: VLLM_NO_USAGE_STATS
              value: "1"
            - name: XDG_CACHE_HOME
              value: /runtime-cache/xdg
            - name: TRANSFORMERS_OFFLINE
              value: "1"
          ports:
            - containerPort: 8000
              name: upstream
              protocol: TCP
          resources:
            limits:
              cpu: "32"
              ephemeral-storage: 32Gi
              memory: 256Gi
              nvidia.com/gpu: "1"
            requests:
              cpu: "8"
              ephemeral-storage: 8Gi
              memory: 128Gi
              nvidia.com/gpu: "1"
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          startupProbe:
            failureThreshold: 240
            httpGet:
              path: /health
              port: upstream
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /health
              port: upstream
            periodSeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            failureThreshold: 4
            tcpSocket:
              port: upstream
            periodSeconds: 15
            timeoutSeconds: 3
          volumeMounts:
            - mountPath: /model-cache
              name: model-cache
              readOnly: true
            - mountPath: /runtime-cache
              name: runtime-cache
            - mountPath: /tmp
              name: tmp
        - name: bounded-json-adapter
          image: docker.io/vllm/vllm-omni@sha256:6d2630c7d637b699557573f2c3fee8df5d4d0cd718977aa22549ed6a6ef30587
          imagePullPolicy: IfNotPresent
          command:
            - python3
            - /adapter/adapter.py
          env:
            - name: HOME
              value: /tmp/home
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: PYTHONDONTWRITEBYTECODE
              value: "1"
          ports:
            - containerPort: 8080
              name: adapter
              protocol: TCP
          resources:
            limits:
              cpu: "2"
              ephemeral-storage: 1Gi
              memory: 2Gi
            requests:
              cpu: 250m
              ephemeral-storage: 128Mi
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          startupProbe:
            failureThreshold: 30
            httpGet:
              path: /healthz
              port: adapter
            periodSeconds: 2
            timeoutSeconds: 2
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /readyz
              port: adapter
            periodSeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            failureThreshold: 4
            httpGet:
              path: /healthz
              port: adapter
            periodSeconds: 15
            timeoutSeconds: 3
          volumeMounts:
            - mountPath: /adapter
              name: adapter
              readOnly: true
            - mountPath: /tmp
              name: adapter-tmp
      volumes:
        - configMap:
            defaultMode: 292
            name: cosmos3-nano-adapter
          name: adapter
        - emptyDir:
            sizeLimit: 2Gi
          name: adapter-tmp
        - name: model-cache
          persistentVolumeClaim:
            claimName: cosmos3-nano-cache
        - emptyDir:
            sizeLimit: 16Gi
          name: runtime-cache
        - emptyDir:
            sizeLimit: 32Gi
          name: tmp
---
apiVersion: v1
kind: Service
metadata:
  name: cosmos3-nano
  namespace: fs2-models
  labels:
    app.kubernetes.io/component: model-runtime
    app.kubernetes.io/instance: cosmos3-nano
    app.kubernetes.io/managed-by: fs2-serve-models
    app.kubernetes.io/name: cosmos3-nano
    app.kubernetes.io/part-of: fs2-serve
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: adapter
    - name: upstream
      port: 8000
      protocol: TCP
      targetPort: upstream
  selector:
    app.kubernetes.io/instance: cosmos3-nano
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: cosmos3-nano
  namespace: fs2-models
  labels:
    app.kubernetes.io/name: cosmos3-nano
    app.kubernetes.io/part-of: fs2-serve
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/instance: cosmos3-nano
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: fs2-system
      ports:
        - port: 8080
          protocol: TCP
        - port: 8000
          protocol: TCP
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - port: 443
          protocol: TCP
